<%# if policy(Favorite.new).create? %>
  <div class="favorite">
    <% if favorite = current_user.favorited(bookmark) %>
      <%= link_to [bookmark, favorite], class: 'btn btn-danger', method: :delete do %>
        <i class="glyphicon glyphicon-star-empty"> </i>&nbsp; Unfavorite
      <% end %>
    <% else %>
      <%= link_to [bookmark, Favorite.new], class: 'btn btn-primary', method: :post do %>
        <i class="glyphicon glyphicon-star"> </i>&nbsp; Favorite
      <% end %>
    <% end %>
  </div>
<%# end %>